---
const { eyebrow, title, description } = Astro.props;
---

<header class="section-header fade-rise">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  {description && <p class="section-copy">{description}</p>}
</header>
